# ------ Constants ------

TCP_DATA_DIR = "tcp_data"

# Default amount of historical test run results to store per test
DEFAULT_HIST_LEN = 10
DEFAULT_WEIGHT = "1-0"

ALL_TESTS = ["PYTEST:DEP:ALL_TESTS"]
